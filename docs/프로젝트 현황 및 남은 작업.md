# 프로젝트 현황 및 남은 작업

> 최종 업데이트: 2026-02-20
> 프로젝트: 웹툰 리뷰 챗봇 프로토타입

---

## 1. 전체 완성도 요약

| 영역 | 완성도 | 비고 |
|---|:---:|---|
| 백엔드 API | **100%** | 127개 엔드포인트 전체 구현 (사용자 91 + 관리자 36) — TTS 3개 + ImageGen 2개 추가 |
| DB 스키마 | **100%** | 36 테이블 + 15개 Alembic 마이그레이션 완료 |
| 프론트엔드 UI | **100%** | 29 페이지 + 57 컴포넌트, Tailwind CSS 전환 완료 |
| 테스트 | **100%** | Backend 346+17개, Frontend 37개 컴포넌트, E2E ~111+35개 |
| 인프라 | **100%** | Docker Compose + CI/CD + 관측성 스택 완료 |
| Live2D 연동 | **90%** | PixiJS + pixi-live2d-display 실제 SDK 통합 완료 |
| 성인인증 | **70%** | 테스트용 검증 로직 + 모달 UI 완료, 실제 인증사 연동만 남음 |
| RunPod 추론 | **90%** | SGLang 사이드카 핸들러 구현 완료, 실제 배포만 남음 |
| SSE 보안 | **100%** | POST + Authorization 헤더 방식 전환 완료 |
| CI/CD | **100%** | GitHub Actions 11개 job (6 기존 + 5 보안 스캐닝) 구성 완료 |
| Rate Limiting | **100%** | Redis 슬라이딩 윈도우 미들웨어 구현 완료 |
| 사용량 할당 | **100%** | Quota 모델 + 서비스 + API + 채팅 연동 완료 |
| 배치 파이프라인 | **100%** | 비동기 스케줄러 + 관리자 API 구현 완료 |
| 관측성 | **100%** | Langfuse(self-hosted) + Prometheus + Grafana 대시보드 완료 |
| 프론트엔드 최적화 | **100%** | Tailwind CSS, 동적 import, React.memo, 빌드 최적화 완료 |
| 모바일 반응형 | **100%** | 사이드바 드로어, 모달/그리드 반응형, 터치 최적화 완료 |

---

## 2. 구현 완료 항목

### 2.1 백엔드 (Python / FastAPI)

- **API 엔드포인트 143개** 구현 완료 (사용자 107 + 관리자 36)
  - Auth: 회원가입, 로그인, 내 정보 조회, 성인인증(요청/상태/철회), 동의이력 — 7개
  - Chat: 세션 생성, 세션 목록, 메시지 조회, 메시지 전송, SSE 스트리밍, 메시지 재생성/수정/브랜칭, 인사말 시스템 — 12개
  - Personas: 목록, 생성, 조회, 수정, 삭제, 태그/검색/정렬/카테고리 — 8개
  - User Personas: 사용자 페르소나 CRUD + 프롬프트 통합 — 6개
  - Favorites: 즐겨찾기 추가/해제, like_count 동기화 — 4개
  - Relationships: 호감도/관계 조회, 7단계 시스템, 감정 기반 증감 — 5개
  - Notifications: 알림 CRUD, 읽음/전체읽음 — 5개
  - Character Cards: SillyTavern 호환 Import/Export — 3개
  - Memories: 메모리 CRUD, 자동 메모리 추출 — 5개
  - Lorebook: 페르소나별 조회, 생성, 수정, 삭제 — 4개
  - Webtoons: 목록, 상세 조회, 에피소드 상세 — 3개
  - Models: 사용 가능 모델 목록, 선호 모델 설정 — 2개
  - Policy: 스포일러 설정 조회/수정 — 2개
  - Usage: 내 사용량 요약, 일별 히스토리, 내 할당 상태 — 3개
  - Credits: 크레딧 잔액/거래, 구매 — 4개
  - Subscriptions: 구독 플랜 조회, 가입, 해지 — 4개
  - Board: 커뮤니티 게시글/댓글/리액션 CRUD — 9개
  - Admin/Users: 사용자 목록, 역할 변경, 일괄 삭제 — 3개
  - Admin/Personas: 모더레이션 목록, 상태 변경 — 2개
  - Admin/LLM Models: 전체 목록, 등록, 수정, 활성/비활성 토글 — 4개
  - Admin/Usage: 전체 통계, 사용자별 사용량, 할당 목록, 할당 설정 — 4개
  - Admin/Monitoring: 시스템 통계, 활동 로그 — 2개
  - Admin/Content: 웹툰 목록, 웹툰 생성, 에피소드 생성, Live2D 모델 업로드 — 4개
  - Admin/System: 설정 조회, 배치 작업 제출/목록/상세 — 4개
  - Admin/Credits: 크레딧 원장 조회, 지급 — 3개
  - Admin/Subscriptions: 구독 플랜/사용자 조회 — 3개
  - Admin/Board: 게시글/댓글 모더레이션 — 3개
  - Admin/Agents: 에이전트 관리 — 3개
  - Character Pages: 캐릭터 페이지 CRUD, 팔로우/언팔로우 — 6개
  - Character Chats: 캐릭터 간 1:1 대화 세션/메시지 CRUD — 8개
  - Pending Posts: 승인 큐 목록/승인/반려 — 5개
  - World Events: 세계관 이벤트 CRUD (관리자) — 4개
  - Uploads: 파일 업로드 — 1개
  - Health: 헬스체크 — 1개

- **서비스 계층 31개** — chat, persona, lorebook, policy, usage, inference_client, rag, review, moderation, user, adult_verify, quota, batch_scheduler, credit, subscription, board, agent_scheduler, agent_activity, user_persona, favorite, relationship, notification, character_card, tts(스텁), image_gen(스텁), credit, subscription, board, character_page, character_chat, pending_post, world_event

- **NLP 파이프라인 5개** — KoreanNLP(Kiwi), PII(Presidio), Emotion(KcELECTRA), Embedding(BGE-M3), Reranker

- **배치 파이프라인** — 에피소드 감정 분석 + 임베딩 사전 계산 비동기 스케줄러

- **ORM 모델 39개** — users, personas, chat_sessions, chat_messages, llm_models, token_usage_logs, usage_quotas, lorebook_entries, webtoons, episodes, subscription_plans, user_subscriptions, credit_ledger, credit_costs, boards, board_posts, board_comments, board_reactions, persona_lounge_configs, agent_activity_logs, user_personas, persona_favorites, persona_relationships, notifications, pending_posts, character_chat_sessions, character_chat_messages, world_events, video_generations, persona_reports 등

- **LLM 추론 클라이언트** — 4개 provider(RunPod, OpenAI, Anthropic, Google) SSE 스트리밍 지원

- **Rate Limiting** — Redis 슬라이딩 윈도우 미들웨어 (auth: 20/분, chat: 30/분, api: 60/분, admin: 120/분)

- **사용량 할당(Quota)** — 일일/월간 토큰 한도 + 월간 비용 한도, 초과 시 429 반환

- **관측성** — Langfuse 트레이싱, Sentry 에러 추적, Prometheus 메트릭 + 커스텀 LLM/파이프라인 메트릭

- **성인인증** — 4개 방식(자가선언/휴대폰/카드/SSO) 테스트용 검증 로직 (생년 기반 연령 확인, 테스트 인증코드)

- **사용자 페르소나** — 사용자 정의 페르소나 CRUD + 프롬프트 컴파일러 통합

- **즐겨찾기** — 페르소나 즐겨찾기 추가/해제 + like_count 동기화

- **호감도/관계 시스템** — 7단계 관계, 0-1000 호감도 점수, 감정 기반 자동 증감

- **알림** — 알림 CRUD + 읽음/전체읽음 처리

- **캐릭터 카드 Import/Export** — SillyTavern 호환 JSON 형식

- **메모리 대시보드** — 사용자 메모리 CRUD + 자동 메모리 추출 (5번째 메시지마다)

- **크리에이터 대시보드** — 내 페르소나 통계 (사용량, 즐겨찾기 수 등)

- **메시지 재생성/수정/브랜칭** — parent_id 기반 메시지 트리, is_active 상태, sibling 탐색

- **인사말 시스템** — greeting_message → 세션 시작 시 첫 assistant 메시지 자동 생성

- **태그/검색/카테고리** — 페르소나 ARRAY 태그 필터, ILIKE 검색, 다중 정렬, 8개 카테고리(로맨스/액션/판타지/일상/공포/코미디/드라마/SF) 필터 지원

- **세션 관리 확장** — title, pin, delete(soft), archive 기능 추가

- **프롬프트 컴파일러 통합** — 8-layer prompt (정책 → 페르소나 → 관계 → 사용자 페르소나 → 기억 → 예시대화 → 세션 요약 → 근거 번들)

- **TTS/이미지 생성 스텁** — 인터페이스만 정의, NotImplementedError (실제 구현 대기)

- **커뮤니티 게시판** — 게시글/댓글/리액션 CRUD + 페이징/필터링

- **크레딧/구독 시스템** — 크레딧 잔액/차감/구매/환불, 구독 플랜 관리

### 2.2 프론트엔드 (Next.js 15 / React 19 / TypeScript)

- **사용자 페이지 17개**
  - 채팅(`chat/[sessionId]`) — Live2D + 배경 + SSE 스트리밍 + 메시지 재생성/수정/브랜칭 + 인사말
  - 세션 목록(`sessions`) — title, pin, delete(soft), archive
  - 페르소나 목록/생성/수정/로어북(`personas/*`) — 태그/검색/정렬/카테고리 필터
  - 즐겨찾기(`favorites`) — 추가/해제 + like_count 동기화
  - 호감도/관계(`relationships`) — 7단계, 0-1000 호감도, 감정 기반 증감
  - 알림(`notifications`) — CRUD + 읽음/전체읽음
  - 마이페이지(`mypage`) — 7탭: profile, settings, usage, subscription, user-persona, memories, creator
  - 커뮤니티(`community`) — 게시글 피드 + 상세(`community/post/[id]`)
  - 프로필(`profile`)
  - 캐릭터 페이지(`character/[id]`) — 인스타 스타일 프로필 + 게시물 피드 + 팔로우
  - 캐릭터 채팅(`character-chats`) — 1:1 대화 요청/수락/턴제 대화
  - 승인 큐(`pending-posts`) — 수동 퍼블리싱 대기 게시물 관리
  - 사용량(`usage`) — 토큰 사용량 대시보드

- **관리자 페이지 11개**
  - 대시보드, 사용자 관리, 페르소나 모더레이션
  - 콘텐츠 관리, LLM 모델 관리, 사용량/과금
  - 정책 설정, 모니터링
  - 신고 관리, 세계관 이벤트 관리

- **컴포넌트 57개** (Tailwind CSS)
  - Chat: ChatWindow(memo), MessageInput(memo+useCallback), SpoilerGate, MessageActions(재생성/수정/브랜칭), RelationshipBar(호감도 표시)
  - Persona: PersonaForm(zod), LorebookEditor, Live2DPicker, AgeRatingBadge(memo), TagChips(태그 필터)
  - Live2D: Live2DCanvas(PixiJS+Cubism4), BackgroundLayer(memo), Live2DController
  - Usage: UsageChart(recharts, dynamic import), ModelCostCard(memo)
  - Auth: AdultVerifyModal(4방식 탭), AgeGateModal
  - Admin: Sidebar, DataTable, StatCard(memo), UserDetailDrawer
  - Layout: UserSidebar, ErrorBoundary, NotificationBell(알림 벨)
  - Mypage: ProfileTab, SettingsTab, UsageTab, SubscriptionTab, UserPersonaTab(사용자 페르소나), MemoriesTab(메모리 대시보드), CreatorTab(크리에이터 통계)
  - Community: PostCard, PostEditor, CommentTree, ReactionButton, PersonaAvatar, PersonaLoungeSettings, ActivityReport
  - Credits: CreditBadge, PurchaseModal
  - Subscription: SubscriptionCard
  - UI: Toast, Skeleton, EmptyState, ConfirmDialog
  - Character: CharacterPageHeader, FollowButton, CharacterPostFeed, CharacterChatRoom, PendingPostCard, WorldEventBanner

- **성능 최적화**
  - Tailwind CSS — 인라인 스타일 전체 제거, CSS 번들 최소화
  - Dynamic Import — Live2D(PixiJS ~500KB), Recharts(~300KB) 코드 스플리팅
  - React.memo + useCallback — SSE 스트리밍 중 불필요한 리렌더링 감소
  - Webpack splitChunks — 벤더 라이브러리 별도 청크 분리

- **모바일 반응형**
  - 사이드바 드로어 — 모바일에서 fixed overlay + 백드롭, 데스크톱에서 고정 사이드바
  - 모달/그리드 반응형 — max-w-[90vw], grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 패턴
  - MobileHeader — 햄버거 메뉴 + 앱 타이틀 (md:hidden)
  - 카드/테이블 — 스크롤 가능한 테이블, 반응형 카드 그리드
  - 터치 최적화 — flex-wrap 필터, 스크롤 탭, 반응형 패딩

- **상태 관리** — Zustand 스토어 13개 (chat, persona, live2d, user, toast, community, credit, notification, ui, characterPage, characterChat, pendingPost, worldEvent)

- **SSE 클라이언트** — POST 기반 fetch + ReadableStream (Authorization 헤더 포함)

### 2.3 인프라

- `docker-compose.yml` — 7개 서비스
  - PostgreSQL 16 + pgvector, Redis 7, Backend, Frontend
  - Prometheus (메트릭 수집, 15초 간격)
  - Grafana (8패널 대시보드 자동 프로비저닝)
  - Langfuse (self-hosted LLM 관측성)
- `infra/docker/Dockerfile.backend` — Python 3.12-slim 기반
- `infra/docker/Dockerfile.frontend` — Node 20-alpine 기반
- `infra/nginx/nginx.conf` — 리버스 프록시 + SSE 지원
- `infra/runpod/handler.py` — SGLang 사이드카 패턴 핸들러 + Dockerfile
- `infra/prometheus/prometheus.yml` — 백엔드 메트릭 스크래핑 설정
- `infra/grafana/` — 데이터소스/대시보드 자동 프로비저닝
- `.github/workflows/ci.yml` — 6개 병렬 job (lint, unit, integration, frontend lint/test/build)
- `backend/alembic/` — 15개 마이그레이션 (초기 17테이블 → usage_quotas → credits/subscriptions → community board → agent system → RP 기능 테이블/컬럼 → deleted 세션 상태 → 페르소나 카테고리 → 익명 설정 → LLM 크레딧 → 비디오 생성 → 신고 시스템 → 임시 차단 → 캐릭터 페이지 시스템 → 크레딧 비용 매핑)

### 2.4 테스트

- **백엔드 단위 테스트**
  - 파이프라인: PII(14), Emotion(8), Embedding(7), KoreanNLP(10), Reranker(8)
  - 프롬프트: Compiler(9)
  - 서비스: ChatService(4), InferenceClient(14), UsageService(4), QuotaService(11), BatchScheduler(20), CreditService, SubscriptionService, BoardService, AgentActivityService
  - 코어: RateLimit(27)
- **백엔드 통합 테스트** — 전체 API 엔드포인트 커버 (auth, chat, personas, lorebook, webtoons, models, admin, credits, subscriptions, board, lounge, policy)
- **백엔드 전체: 346개 테스트 통과** (29개 테스트 파일)
- **프론트엔드 컴포넌트 테스트 37개** — 스토어, AgeRatingBadge, AdultVerifyModal, AgeGateModal 등
- **E2E 테스트 ~111개** — Playwright 기반 (API 모킹)
  - auth.spec.ts (12) — 로그인, 회원가입, 보호 라우트
  - persona.spec.ts (28) — 페르소나 CRUD, 연령등급
  - chat.spec.ts (24) — 채팅 UI, SSE 스트리밍
  - admin.spec.ts (30) — 관리자 대시보드 전체
  - adult-verify.spec.ts (17) — 성인인증 모달, 방식 선택
- TypeScript 컴파일 에러 0건 (`tsc --noEmit` 통과)
- `next build` 프로덕션 빌드 성공 (22 routes)
- **테스트 시나리오 문서** — `docs/테스트 시나리오.md` (137개 화면별 테스트 시나리오)

---

## 3. 남은 작업

### 3.1 구현 관련 남은 작업

#### RP 기능 DB 마이그레이션 ✅ 완료
- **마이그레이션 적용 완료:** `f5a6b7c8d9e0_add_rp_features.py`, `a1b2c3d4e5f6_add_deleted_session_status.py`
- **personas 테이블 컬럼 추가:** description, greeting_message, scenario, example_dialogues, tags, chat_count, like_count
- **chat_sessions 컬럼 추가:** title, is_pinned, user_persona_id
- **chat_messages 컬럼 추가:** parent_id, is_active, is_edited, edited_at
- **신규 테이블:** user_personas, persona_favorites, persona_relationships, notifications
- **chat_sessions.status CHECK에 'deleted' 추가**

#### 페르소나 카테고리 필드 ✅ 완료
- **마이그레이션 적용 완료:** `b2c3d4e5f6a7_add_persona_category.py`
- **personas 테이블:** `category VARCHAR(30)` 컬럼 + `idx_personas_category` 인덱스 추가
- **백엔드:** ORM 모델, Pydantic 스키마(Create/Update/Response), 서비스 필터링, API 쿼리 파라미터 구현
- **프론트엔드:** 공유 상수(`constants/categories.ts`), PersonaForm 카테고리 셀렉트, 탐색 페이지 카테고리 필터 칩
- **8개 카테고리:** romance, action, fantasy, daily, horror, comedy, drama, scifi

#### 모바일 반응형 ✅ 완료
- **사이드바:** UserSidebar, AdminSidebar를 반응형 드로어로 전환 (모바일: fixed overlay, 데스크톱: 고정)
- **모달:** 전체 모달에 max-w-[90vw], mx-4 적용
- **그리드:** grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 반응형 패턴
- **MobileHeader 컴포넌트:** 햄버거 메뉴 + 앱 타이틀 (md:hidden)
- **uiStore:** 사이드바 open/close 상태 관리 (Zustand)

#### TTS 실제 구현 ✅ 완료
- **구현 완료:** OpenAI TTS + ElevenLabs 멀티 프로바이더 지원
- **파일:** `backend/app/services/tts_service.py`, `backend/app/api/tts.py`, `backend/app/schemas/tts.py`
- **엔드포인트:** POST `/api/tts/synthesize`, POST `/api/tts/synthesize-message`, GET `/api/tts/voices`
- **프론트엔드:** MessageActions 컴포넌트에 TTS 재생/정지 버튼 활성화
- **테스트:** 8개 단위 테스트 (`backend/tests/unit/services/test_tts_service.py`)

#### AI 이미지 생성 실제 구현 ✅ 완료
- **구현 완료:** OpenAI DALL-E 3 + Stability AI 멀티 프로바이더 지원
- **파일:** `backend/app/services/image_gen_service.py`, `backend/app/api/image_gen.py`, `backend/app/schemas/image_gen.py`
- **엔드포인트:** POST `/api/image-gen/generate`, GET `/api/image-gen/styles`
- **스타일:** anime, realistic, cartoon, oil_painting, watercolor, pixel
- **테스트:** 9개 단위 테스트 (`backend/tests/unit/services/test_image_gen_service.py`)

#### E2E 테스트 (Playwright) 확대 ✅ 완료
- **추가:** 6개 E2E 테스트 파일, ~35개 시나리오 추가
  - `favorites.spec.ts` — 즐겨찾기 CRUD, 빈 상태, 연령등급 배지
  - `notifications.spec.ts` — 알림 목록, 읽음 처리, 전체 읽음
  - `community.spec.ts` — 게시글 목록, 상세, 좋아요/댓글 수
  - `credits.spec.ts` — 크레딧 잔액, 거래 내역
  - `relationships.spec.ts` — 호감도 목록, 관계 단계
  - `mypage.spec.ts` — 7탭 전환, 프로필, 사용량, 구독, 메모리, 크리에이터
- **helpers.ts:** 신규 mock 데이터 및 API 라우트 추가 (favorites, notifications, credits, board, relationships, subscriptions, TTS, image-gen 등)

#### CI/CD 파이프라인 보안 스캐닝 추가 ✅ 완료
- **추가:** 5개 보안 스캐닝 job
  - `codeql-analysis` — CodeQL SAST (JavaScript/TypeScript + Python)
  - `dependency-review` — PR 시 의존성 취약점 검토 (high 이상 차단, GPL-3.0/AGPL-3.0 거부)
  - `python-security` — safety (의존성 취약점) + bandit (Python 보안 린트)
  - `npm-audit` — npm audit (high 이상)
  - `secret-scanning` — TruffleHog 시크릿 탐지
- **설정:** `pyproject.toml`에 bandit 설정 추가

#### Redis 영구 저장 설정 ✅ 완료
- **구현 완료:** docker-compose.yml Redis 서비스에 RDB + AOF 영구 저장 설정 추가
  - AOF: `appendonly yes`, `appendfsync everysec`
  - RDB: 900초/1변경, 300초/10변경, 60초/10000변경 스냅샷
  - 메모리 제한: 256MB + allkeys-lru 정책

#### 캐릭터 페이지 시스템 (Phase 1-5) ✅ 완료
- **캐릭터 페이지:** 인스타그램 스타일 캐릭터 프로필 (팔로우, 게시물 피드, 프로필 헤더)
- **캐릭터 간 1:1 대화:** 요청/수락/거절 상태 관리, 턴제 대화 (최대 20턴), LLM 기반 AI 응답
- **수동 퍼블리싱 승인 큐:** auto_publish=false 시 pending_posts에 저장, 소유자/관리자 승인/반려
- **세계관 이벤트:** 관리자 정의 이벤트가 프롬프트 Layer 1.5로 주입, global/board/persona 타입
- **DB 확장:** 4개 신규 테이블 (pending_posts, character_chat_sessions, character_chat_messages, world_events)
- **기존 테이블 수정:** personas(follower_count, is_character_page_enabled), persona_lounge_configs(auto_publish), board_posts(publish_status), notifications(type 확장), credit_costs(action 확장)
- **프론트엔드:** 4개 신규 페이지, 6개 신규 컴포넌트, 4개 신규 스토어
- **마이그레이션:** h8c9d0e1f2g3 (캐릭터 페이지 시스템) + i9d0e1f2g3h4 (크레딧 비용 매핑)

### 3.2 외부 의존 (운영 전 설정 필요)

#### 성인인증 실제 인증사 연동
- **파일:** `backend/app/services/adult_verify_service.py`
- **현재 상태:** 테스트용 검증 로직 구현 완료 (생년 기반 연령 확인, 인증코드 123456)
- **프로덕션 전환 시:**
  - 휴대폰 본인인증 API 연동 (KG이니시스/나이스 등)
  - 카드사 인증 API 연동
  - OAuth SSO 연동
- **의존:** 인증 서비스 업체 계약 및 API 키 발급

#### 프로덕션 배포 (AWS EC2 + RunPod)
- **파일:** `infra/runpod/handler.py`, `Dockerfile`, `requirements.txt`
- **현재 상태:** SGLang 사이드카 패턴 핸들러 구현 완료
- **프로덕션 전환 시:**
  - AWS EC2 t4g.small 인스턴스 프로비저닝 (서울 리전)
  - RunPod 계정 생성 + GPU 엔드포인트 설정
  - Docker 이미지 빌드 및 배포
  - 엔드포인트 ID를 `.env`에 설정
- **의존:** AWS 계정 + RunPod 계정 + A100 80GB GPU 엔드포인트

#### LLM API 키 설정
- **현재 상태:** 4개 provider(RunPod, OpenAI, Anthropic, Google) 클라이언트 구현 완료
- **필요:** 최소 1개 이상의 LLM API 키를 `.env`에 설정
- **의존:** 각 서비스 계정 가입 및 API 키 발급

#### Live2D 에셋 준비
- **현재 상태:** PixiJS + pixi-live2d-display SDK 통합 완료, 모션/감정 매핑 코드 구현
- **필요:**
  - Cubism SDK 4 라이센스 취득
  - Live2D 모델 파일(.model3.json, .moc3) 제작 또는 구매
  - 배경 이미지 에셋 준비
- **의존:** Live2D Inc. 라이센스, 모델 에셋

---

## 4. 사용자가 직접 설정해야 할 사항

### 4.1 환경 변수 파일 생성 (필수)

`.env.example`을 복사하여 `.env` 파일을 생성하고 실제 값을 입력해야 합니다.

```bash
cp .env.example .env
```

#### 환경 변수 목록

| 변수 | 설명 | 기본값 / 비고 |
|---|---|---|
| **DATABASE_URL** | PostgreSQL 비동기 접속 URL | `postgresql+asyncpg://chatbot:chatbot@localhost:5432/chatbot` |
| **DATABASE_SYNC_URL** | PostgreSQL 동기 접속 URL (Alembic용) | `postgresql://chatbot:chatbot@localhost:5432/chatbot` |
| **REDIS_URL** | Redis 접속 URL | `redis://localhost:6379/0` |
| **SECRET_KEY** | JWT 서명용 시크릿 키 | 반드시 랜덤 문자열로 변경 (`openssl rand -hex 32`) |
| **ACCESS_TOKEN_EXPIRE_MINUTES** | JWT 토큰 만료 시간(분) | `60` |
| **RUNPOD_API_KEY** | RunPod API 키 | RunPod 대시보드에서 발급 |
| **RUNPOD_ENDPOINT_ID** | RunPod Serverless 엔드포인트 ID | 엔드포인트 생성 후 확인 |
| **OPENAI_API_KEY** | OpenAI API 키 | GPT-4o 사용 시 필요 |
| **ANTHROPIC_API_KEY** | Anthropic API 키 | Claude 사용 시 필요 |
| **GOOGLE_API_KEY** | Google AI API 키 | Gemini 사용 시 필요 |
| **LANGFUSE_PUBLIC_KEY** | Langfuse 퍼블릭 키 | self-hosted 사용 시 Langfuse UI에서 발급 |
| **LANGFUSE_SECRET_KEY** | Langfuse 시크릿 키 | self-hosted 사용 시 Langfuse UI에서 발급 |
| **LANGFUSE_HOST** | Langfuse 호스트 URL | `http://localhost:3001` |
| **SENTRY_DSN** | Sentry DSN URL | 선택 (없으면 에러 추적 비활성) |
| **APP_ENV** | 앱 환경 | `development` |
| **DEBUG** | 디버그 모드 | `true` |
| **CORS_ORIGINS** | 허용 CORS 출처 | `http://localhost:3000` |
| **RATE_LIMIT_ENABLED** | Rate Limiting 활성화 | `true` |
| **RATE_LIMIT_AUTH** | Auth 엔드포인트 제한 (요청/분) | `20` |
| **RATE_LIMIT_CHAT** | Chat 엔드포인트 제한 (요청/분) | `30` |
| **RATE_LIMIT_API** | 일반 API 제한 (요청/분) | `60` |
| **RATE_LIMIT_ADMIN** | Admin 엔드포인트 제한 (요청/분) | `120` |
| **QUOTA_ENABLED** | 사용량 할당 활성화 | `true` |
| **DEFAULT_DAILY_TOKEN_LIMIT** | 기본 일일 토큰 한도 | `100000` |
| **DEFAULT_MONTHLY_TOKEN_LIMIT** | 기본 월간 토큰 한도 | `2000000` |
| **DEFAULT_MONTHLY_COST_LIMIT** | 기본 월간 비용 한도 ($) | `10.0` |

> **참고:** LLM API 키는 최소 1개 이상 설정해야 채팅 기능이 동작합니다.

### 4.2 Docker 서비스 구동 (필수)

```bash
# 로컬 개발: DB + Redis만 구동
docker-compose up -d db redis

# 관측성 포함: DB + Redis + Prometheus + Grafana + Langfuse
docker-compose up -d db redis prometheus grafana langfuse

# 전체 서비스 구동
docker-compose up -d
```

### 4.3 Alembic 마이그레이션 실행 (필수)

DB 컨테이너 구동 후 마이그레이션을 실행하여 36개 테이블 + 인덱스를 생성합니다.

```bash
cd backend
alembic upgrade head
```

마이그레이션 내용:
- `a3b4fcc66da6` — pgvector 확장 + 17개 테이블 생성
- `b1c2d3e4f5a6` — usage_quotas 테이블 추가
- `c2d3e4f5a6b7` — 크레딧/구독 테이블 추가 (subscription_plans, user_subscriptions, credit_ledger, credit_costs)
- `d3e4f5a6b7c8` — 커뮤니티 게시판 테이블 추가 (boards, board_posts, board_comments, board_reactions)
- `e4f5a6b7c8d9` — 에이전트 시스템 테이블 추가 (persona_lounge_configs, agent_activity_logs)
- `f5a6b7c8d9e0` — RP 기능 테이블/컬럼 추가 (user_personas, persona_favorites, persona_relationships, notifications + personas/chat_sessions/chat_messages 컬럼 확장)
- `a1b2c3d4e5f6` — chat_sessions.status CHECK에 'deleted' 추가
- `b2c3d4e5f6a7` — 페르소나 카테고리 컬럼 추가 (personas.category VARCHAR(30) + idx_personas_category)
- `c3d4e5f6a7b8` — 페르소나 익명 설정 추가
- `d4e5f6a7b8c9` — LLM 모델 크레딧 필드 추가
- `e5f6a7b8c9d0` — 비디오 생성 테이블 추가
- `f6a7b8c9d0e1` — 페르소나 신고 시스템 추가
- `g7b8c9d0e1f2` — 사용자 임시 차단(banned_until) 추가
- `h8c9d0e1f2g3` — 캐릭터 페이지 시스템 (pending_posts, character_chat_sessions, character_chat_messages, world_events + 기존 테이블 컬럼 추가)
- `i9d0e1f2g3h4` — 캐릭터 페이지 크레딧 비용 매핑 추가

### 4.4 Python 환경 (권장)

| 항목 | 현재 | 권장 |
|---|---|---|
| Python 버전 | 3.10.11 | **3.12+** |

- 현재 3.10에서도 동작하도록 호환성 패치(`StrEnum` → `str, Enum`)가 적용되어 있음
- CLAUDE.md 기준 및 Docker 배포 환경은 Python 3.12 사용

### 4.5 Live2D 에셋 준비 (Live2D 사용 시)

Live2D 캐릭터를 실제로 렌더링하려면 아래 에셋이 필요합니다.

```
frontend/public/assets/
├── live2d/
│   └── {모델명}/
│       ├── {모델명}.model3.json    # 모델 정의
│       ├── {모델명}.moc3           # 모션 데이터
│       ├── {모델명}.physics3.json  # 물리 연산
│       ├── textures/              # 텍스처 이미지
│       └── motions/               # 모션 파일
└── backgrounds/
    ├── default.jpg                # 기본 배경
    └── {추가 배경 이미지들}
```

- **Cubism SDK 4 라이센스** — Live2D Inc.에서 별도 취득 필요
  - 무료 SDK: 출시 타이틀 연 1000만엔 이하 매출 시 무료
  - https://www.live2d.com/sdk/license/
- **모델 제작** — Cubism Editor로 직접 제작하거나 에셋 스토어에서 구매

### 4.6 외부 서비스 가입

| 서비스 | 용도 | 필수 여부 | 비고 |
|---|---|:---:|---|
| **RunPod** | Llama 3 70B 추론 | LLM 중 택 1 | 핸들러 구현 완료, 배포만 필요 |
| **OpenAI** | GPT-4o 모델 | 선택 | API 키만 설정하면 즉시 사용 |
| **Anthropic** | Claude 모델 | 선택 | API 키만 설정하면 즉시 사용 |
| **Google AI** | Gemini 모델 | 선택 | API 키만 설정하면 즉시 사용 |
| **Langfuse** | LLM 관측성 | 선택 | self-hosted (docker-compose 포함) |
| **Sentry** | 에러 추적 | 선택 | DSN 설정 시 활성화 |

> **최소 요건:** LLM API 키 1개 이상 (OpenAI/Anthropic/Google 중 하나면 채팅 기능 테스트 가능)

### 4.7 Playwright 브라우저 설치 (E2E 테스트 시)

```bash
cd frontend
npx playwright install chromium
```

---

## 5. 로컬 개발 실행 방법

### 빠른 시작

```bash
# 1. 환경 변수 설정
cp .env.example .env
# .env 파일을 편집하여 실제 값 입력

# 2. Docker 서비스 (DB + Redis) 구동
docker-compose up -d db redis

# 3. DB 마이그레이션
cd backend
alembic upgrade head

# 4. 백엔드 실행
pip install -r requirements-dev.txt   # 개발/테스트 의존성 포함
uvicorn app.main:app --reload --port 8000

# 5. 프론트엔드 실행 (별도 터미널)
cd frontend
npm install
npm run dev
```

### 관측성 대시보드 (선택)

```bash
# Prometheus + Grafana + Langfuse 구동
docker-compose up -d prometheus grafana langfuse

# 접속 URL:
# - Prometheus:  http://localhost:9090
# - Grafana:     http://localhost:3002 (admin / admin)
# - Langfuse:    http://localhost:3001 (최초 접속 시 가입)
# - 백엔드 메트릭: http://localhost:8000/metrics
```

### 테스트 실행

```bash
# 백엔드 전체 테스트 (346개)
pytest backend/tests/ -v

# 백엔드 단위 테스트만
pytest backend/tests/unit/ -v

# 백엔드 통합 테스트만
pytest backend/tests/integration/ -v

# 프론트엔드 컴포넌트 테스트 (37개)
cd frontend && npx vitest run

# TypeScript 타입 체크
cd frontend && npx tsc --noEmit

# E2E 테스트 (~111개, API 모킹 기반)
cd frontend && npx playwright test

# 프로덕션 빌드 검증
cd frontend && npx next build
```

---

## 6. 아키텍처 요약

```
┌─ 프론트엔드 (Next.js 15) ─────────────────────────────────────┐
│  Tailwind CSS + Dynamic Import + React.memo                    │
│  Live2D (PixiJS + pixi-live2d-display + Cubism 4)             │
│  SSE (POST + Authorization header + ReadableStream)            │
│  Playwright E2E ~111개 + Vitest 37개                           │
└───────────────────────┬────────────────────────────────────────┘
                        │ HTTPS + SSE
┌───────────────────────▼────────────────────────────────────────┐
│  EC2 t4g.small (서울)                                           │
│  ┌─ FastAPI ────────────────────────────────────────────────┐  │
│  │  122개 API 엔드포인트 (사용자 86 + 관리자 36)               │  │
│  │  Rate Limiting (Redis 슬라이딩 윈도우)                    │  │
│  │  Usage Quota (일일/월간 토큰+비용 한도)                   │  │
│  │  배치 스케줄러 (감정 분석/임베딩 사전 계산)                │  │
│  │  346개 테스트 전체 통과                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌─ PostgreSQL + pgvector ──┐  ┌─ Redis ────────────────────┐  │
│  │  36개 테이블              │  │  세션/캐시/Rate Limit      │  │
│  └──────────────────────────┘  └────────────────────────────┘  │
│  ┌─ 관측성 ─────────────────────────────────────────────────┐  │
│  │  Prometheus → Grafana (8패널 대시보드)                     │  │
│  │  Langfuse (LLM 트레이싱, self-hosted)                     │  │
│  │  Sentry (에러 추적)                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────────┬────────────────────────────────────────┘
                        │ HTTP (RTT ~150ms)
┌───────────────────────▼────────────────────────────────────────┐
│  RunPod Serverless (미국) / 외부 LLM API                        │
│  ┌─ 모델 라우터 ────────────────────────────────────────────┐  │
│  │  RunPod SGLang (Llama 3 70B) ← 기본                      │  │
│  │  OpenAI API (GPT-4o) ← 선택                              │  │
│  │  Anthropic API (Claude) ← 선택                           │  │
│  │  Google API (Gemini) ← 선택                              │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

---

## 7. CI/CD 파이프라인

`.github/workflows/ci.yml` — Push/PR 시 자동 실행:

| Job | 내용 |
|---|---|
| `backend-lint` | ruff check + format |
| `backend-unit-test` | pytest unit |
| `backend-integration-test` | pytest integration + pgvector 서비스 |
| `frontend-lint` | ESLint + Prettier |
| `frontend-test` | Vitest (37개) |
| `frontend-build` | next build (lint+test 성공 후) |
