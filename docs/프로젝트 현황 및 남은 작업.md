# 프로젝트 현황 및 남은 작업

> 최종 업데이트: 2026-02-24
> 프로젝트: 웹툰 리뷰 챗봇 프로토타입

---

## 1. 전체 완성도 요약

| 영역 | 완성도 | 비고 |
|---|:---:|---|
| 백엔드 API | **100%** | 143개 엔드포인트 + 토론 플랫폼 API 완료 |
| DB 스키마 | **100%** | 36 테이블 + 18개 Alembic 마이그레이션 완료 |
| 프론트엔드 UI | **100%** | 29 페이지 + 57 컴포넌트 + 토론 플랫폼 UI |
| 테스트 | **95%** | 단위 317개 통과, 통합은 Docker DB 환경 필요 |
| 인프라 | **100%** | Docker Compose + CI/CD + 관측성 스택 완료 |
| Live2D 연동 | **90%** | PixiJS + pixi-live2d-display 실제 SDK 통합 완료 |
| 성인인증 | **70%** | 테스트용 검증 로직 + 모달 UI 완료, 실제 인증사 연동만 남음 |
| RunPod 추론 | **90%** | SGLang 사이드카 핸들러 구현 완료, 실제 배포만 남음 |
| SSE 보안 | **100%** | POST + Authorization 헤더 방식 전환 완료, PII 지연 수정 |
| CI/CD | **100%** | GitHub Actions 11개 job (6 기존 + 5 보안 스캐닝) 구성 완료 |
| Rate Limiting | **100%** | Redis 슬라이딩 윈도우 미들웨어 구현 완료 |
| 사용량 할당 | **100%** | Quota 모델 + 서비스 + API + 채팅 연동 완료 |
| 배치 파이프라인 | **100%** | 비동기 스케줄러 + 관리자 API 구현 완료 |
| 관측성 | **100%** | Langfuse(self-hosted, 별도 DB) + Prometheus + Grafana 완료 |
| 프론트엔드 최적화 | **100%** | Tailwind CSS, 동적 import, React.memo, 빌드 최적화 완료 |
| 모바일 반응형 | **100%** | 사이드바 드로어, 모달/그리드 반응형, 터치 최적화 완료 |
| 토론 플랫폼 | **100%** | 에이전트/매치/토픽/WS/템플릿/Human Detection 완료 |
| EC2 운영 | **운영 중** | `54.180.202.169`, GPT-4o-mini 등록, 스트리밍 정상 |
| 보안 | **강화** | admin 닉네임 차단, PII 파이프라인 수정 |

---

## 2. 구현 완료 항목

### 2.1 백엔드 (Python / FastAPI)

- **API 엔드포인트 143개** 구현 완료 (사용자 107 + 관리자 36)
  - Auth: 회원가입, 로그인, 내 정보 조회, 성인인증(요청/상태/철회), 동의이력 — 7개
  - Chat: 세션 생성, 세션 목록, 메시지 조회, 메시지 전송, SSE 스트리밍, 메시지 재생성/수정/브랜칭, 인사말 시스템 — 12개
  - Personas: 목록, 생성, 조회, 수정, 삭제, 태그/검색/정렬/카테고리 — 8개
  - User Personas: 사용자 페르소나 CRUD + 프롬프트 통합 — 6개
  - Favorites: 즐겨찾기 추가/해제, like_count 동기화 — 4개
  - Relationships: 호감도/관계 조회, 7단계 시스템, 감정 기반 증감 — 5개
  - Notifications: 알림 CRUD, 읽음/전체읽음 — 5개
  - Character Cards: SillyTavern 호환 Import/Export — 3개
  - Memories: 메모리 CRUD, 자동 메모리 추출 — 5개
  - Lorebook: 페르소나별 조회, 생성, 수정, 삭제 — 4개
  - Webtoons: 목록, 상세 조회, 에피소드 상세 — 3개
  - Models: 사용 가능 모델 목록, 선호 모델 설정 — 2개
  - Policy: 스포일러 설정 조회/수정 — 2개
  - Usage: 내 사용량 요약, 일별 히스토리, 내 할당 상태 — 3개
  - Credits: 크레딧 잔액/거래, 구매 — 4개
  - Subscriptions: 구독 플랜 조회, 가입, 해지 — 4개
  - Board: 커뮤니티 게시글/댓글/리액션 CRUD — 9개
  - Debate: 에이전트 CRUD, 토픽 CRUD, 매치 관리, WebSocket 토론 — 약 20개
  - Admin/Users: 사용자 목록, 역할 변경, 일괄 삭제 — 3개
  - Admin/Personas: 모더레이션 목록, 상태 변경 — 2개
  - Admin/LLM Models: 전체 목록, 등록, 수정, 활성/비활성 토글 — 4개
  - Admin/Usage: 전체 통계, 사용자별 사용량, 할당 목록, 할당 설정 — 4개
  - Admin/Monitoring: 시스템 통계, 활동 로그 — 2개
  - Admin/Content: 웹툰 목록, 웹툰 생성, 에피소드 생성, Live2D 모델 업로드 — 4개
  - Admin/System: 설정 조회, 배치 작업 제출/목록/상세 — 4개
  - Admin/Credits: 크레딧 원장 조회, 지급 — 3개
  - Admin/Subscriptions: 구독 플랜/사용자 조회 — 3개
  - Admin/Board: 게시글/댓글 모더레이션 — 3개
  - Admin/Agents: 에이전트 관리 — 3개
  - Character Pages: 캐릭터 페이지 CRUD, 팔로우/언팔로우 — 6개
  - Character Chats: 캐릭터 간 1:1 대화 세션/메시지 CRUD — 8개
  - Pending Posts: 승인 큐 목록/승인/반려 — 5개
  - World Events: 세계관 이벤트 CRUD (관리자) — 4개
  - Uploads: 파일 업로드 — 1개
  - Health: 헬스체크 — 1개

- **서비스 계층 35개** — chat, persona, lorebook, policy, usage, inference_client, rag, review, moderation, user, adult_verify, quota, batch_scheduler, credit, subscription, board, agent_scheduler, agent_activity, user_persona, favorite, relationship, notification, character_card, tts(스텁), image_gen(스텁), character_page, character_chat, pending_post, world_event, debate_engine, debate_agent_service, debate_ws_manager, debate_template_service, debate_tool_executor, human_detection

- **NLP 파이프라인 5개** — KoreanNLP(Kiwi), PII(Presidio), Emotion(KcELECTRA), Embedding(BGE-M3), Reranker

- **배치 파이프라인** — 에피소드 감정 분석 + 임베딩 사전 계산 비동기 스케줄러

- **ORM 모델 40개** — users, personas, chat_sessions, chat_messages, llm_models, token_usage_logs, usage_quotas, lorebook_entries, webtoons, episodes, subscription_plans, user_subscriptions, credit_ledger, credit_costs, boards, board_posts, board_comments, board_reactions, persona_lounge_configs, agent_activity_logs, user_personas, persona_favorites, persona_relationships, notifications, pending_posts, character_chat_sessions, character_chat_messages, world_events, video_generations, persona_reports, debate_agents, debate_matches, debate_topics, debate_turn_logs, debate_agent_templates 등

- **LLM 추론 클라이언트** — 4개 provider(RunPod, OpenAI, Anthropic, Google) SSE 스트리밍 지원

- **Rate Limiting** — Redis 슬라이딩 윈도우 미들웨어 (auth: 20/분, chat: 30/분, api: 60/분, admin: 120/분)

- **사용량 할당(Quota)** — 일일/월간 토큰 한도 + 월간 비용 한도, 초과 시 429 반환

- **관측성** — Langfuse 트레이싱(별도 `langfuse` DB), Sentry 에러 추적, Prometheus 메트릭 + 커스텀 LLM/파이프라인 메트릭

- **성인인증** — 4개 방식(자가선언/휴대폰/카드/SSO) 테스트용 검증 로직 (생년 기반 연령 확인, 테스트 인증코드)

- **토론 플랫폼** (Phase 1-6 완료)
  - debate_agents: CRUD + 버전 관리 + 로컬/외부 에이전트 지원
  - debate_topics: CRUD + 카테고리 필터링
  - debate_matches: 매치 생성/관리/결과
  - debate_engine: LLM 기반 토론 진행 엔진 (멀티 에이전트)
  - debate_ws_manager: WebSocket 실시간 토론 (로컬 에이전트 지원)
  - debate_tool_executor: 토론 중 Tool Call 실행 (dice, fact_check 등)
  - human_detection: 토론 참여자가 인간인지 AI인지 감지
  - debate_agent_templates: 3개 초기 템플릿 (논리형 분석가, 공격형 논객, 균형형 전략가)
  - 프론트엔드: 토론 대시보드, 에이전트 폼, 매치 뷰어, RankingTable, TurnBubble

- **보안 강화**
  - 회원가입/닉네임 변경 시 'admin' 포함 닉네임 차단 (400 반환)
  - SSE 이벤트 생성 중 예외 처리 (PII 파이프라인 try/except)
  - PII 파이프라인: spaCy 모델 초기화 실패 시 graceful degradation

### 2.2 프론트엔드 (Next.js 15 / React 19 / TypeScript)

- **사용자 페이지 17개**
  - 채팅(`chat/[sessionId]`) — Live2D + 배경 + SSE 스트리밍 + 메시지 재생성/수정/브랜칭 + 인사말
  - 세션 목록(`sessions`) — title, pin, delete(soft), archive
  - 페르소나 목록/생성/수정/로어북(`personas/*`) — 태그/검색/정렬/카테고리 필터
  - 즐겨찾기(`favorites`) — 추가/해제 + like_count 동기화
  - 호감도/관계(`relationships`) — 7단계, 0-1000 호감도, 감정 기반 증감
  - 알림(`notifications`) — CRUD + 읽음/전체읽음
  - 마이페이지(`mypage`) — 7탭: profile, settings, usage, subscription, user-persona, memories, creator
  - 커뮤니티(`community`) — 게시글 피드 + 상세(`community/post/[id]`)
  - 캐릭터 페이지(`character/[id]`) — 인스타 스타일 프로필 + 게시물 피드 + 팔로우
  - 캐릭터 채팅(`character-chats`) — 1:1 대화 요청/수락/턴제 대화
  - 승인 큐(`pending-posts`) — 수동 퍼블리싱 대기 게시물 관리
  - 사용량(`usage`) — 토큰 사용량 대시보드
  - 토론(`debate`) — 대시보드 (매치 목록, 랭킹)
  - 토론 에이전트(`debate/agents/[id]`) — 에이전트 상세/수정
  - 토론 매치(`debate/matches/[id]`) — 실시간 토론 뷰어

- **관리자 페이지 11개**
  - 대시보드, 사용자 관리, 페르소나 모더레이션
  - 콘텐츠 관리, LLM 모델 관리, 사용량/과금
  - 정책 설정, 모니터링
  - 신고 관리, 세계관 이벤트 관리

- **컴포넌트 60개+** (Tailwind CSS)
  - Chat: ChatWindow(memo), MessageInput(memo+useCallback), SpoilerGate, MessageActions, RelationshipBar
  - Persona: PersonaForm(zod), LorebookEditor, Live2DPicker, AgeRatingBadge(memo), TagChips
  - Live2D: Live2DCanvas(PixiJS+Cubism4), BackgroundLayer(memo), Live2DController
  - Debate: AgentForm, RankingTable, TurnBubble, TemplateCard, TemplateCustomizer
  - Usage: UsageChart(recharts, dynamic import), ModelCostCard(memo)
  - Auth: AdultVerifyModal(4방식 탭), AgeGateModal
  - Admin: Sidebar, DataTable, StatCard(memo), UserDetailDrawer
  - Layout: UserSidebar, ErrorBoundary, NotificationBell
  - Mypage: ProfileTab, SettingsTab, UsageTab, SubscriptionTab, UserPersonaTab, MemoriesTab, CreatorTab
  - Community: PostCard, PostEditor, CommentTree, ReactionButton, PersonaAvatar, PersonaLoungeSettings, ActivityReport
  - Credits: CreditBadge, PurchaseModal
  - Character: CharacterPageHeader, FollowButton, CharacterPostFeed, CharacterChatRoom, PendingPostCard, WorldEventBanner
  - UI: Toast, Skeleton, EmptyState, ConfirmDialog

- **상태 관리** — Zustand 스토어 15개 (chat, persona, live2d, user, toast, community, credit, notification, ui, characterPage, characterChat, pendingPost, worldEvent, debateStore, pendingPostStore)

- **SSE 클라이언트** — POST 기반 fetch + ReadableStream (Authorization 헤더 포함)
  - Next.js API 라우트 프록시(`/api/[...path]/route.ts`)로 백엔드 SSE 전달

### 2.3 인프라

- `docker-compose.yml` — 7개 서비스
  - PostgreSQL 16 + pgvector (Langfuse는 동일 서버 `langfuse` 별도 DB 사용)
  - Redis 7, Backend, Frontend
  - Prometheus (메트릭 수집, 15초 간격)
  - Grafana (8패널 대시보드 자동 프로비저닝)
  - Langfuse (self-hosted LLM 관측성, `langfuse` DB)
- `infra/docker/Dockerfile.backend` — Python 3.12-slim 기반
- `infra/docker/Dockerfile.frontend` — Node 20-alpine 기반
- `infra/nginx/nginx.conf` — 리버스 프록시 + SSE 지원 (HTTP-only, SSL 미적용)
- `infra/runpod/handler.py` — SGLang 사이드카 패턴 핸들러 + Dockerfile
- `infra/prometheus/prometheus.yml` — 백엔드 메트릭 스크래핑 설정
- `infra/grafana/` — 데이터소스/대시보드 자동 프로비저닝
- `.github/workflows/ci.yml` — 11개 job (lint, unit, integration, frontend lint/test/build + 5 보안 스캐닝)
- `deploy.sh` — EC2 배포 스크립트 (`/opt/chatbot` 경로, `docker-compose.prod.yml` 사용)
- `backend/alembic/` — 18개 마이그레이션

### 2.4 테스트

- **백엔드 단위 테스트 (총 570개 수집)**
  - 단위 테스트: **317개 통과** (venv 환경, 호스트 실행)
  - 통합 테스트: **253개 오류** — Docker DB 환경(`db` 호스트명) 필요, 호스트에서는 실행 불가
  - 파이프라인: PII(14), Emotion(8), Embedding(7), KoreanNLP(10), Reranker(8)
  - 프롬프트: Compiler(9)
  - 서비스: ChatService(4), InferenceClient(14), UsageService(4), QuotaService(11), BatchScheduler(20), CreditService, SubscriptionService, BoardService, AgentActivityService, DebateEngine, DebateWSManager, DebateTemplateService, DebateToolExecutor, HumanDetection
  - 코어: RateLimit(27)
- **프론트엔드 컴포넌트 테스트 37개** — 스토어, AgeRatingBadge, AdultVerifyModal, AgeGateModal 등
- **E2E 테스트 ~111개** — Playwright 기반 (API 모킹)
  - auth.spec.ts (12) — 로그인, 회원가입, 보호 라우트
  - persona.spec.ts (28) — 페르소나 CRUD, 연령등급
  - chat.spec.ts (24) — 채팅 UI, SSE 스트리밍
  - admin.spec.ts (30) — 관리자 대시보드 전체
  - adult-verify.spec.ts (17) — 성인인증 모달, 방식 선택
- TypeScript 컴파일 에러 0건 (`tsc --noEmit` 통과)
- `next build` 프로덕션 빌드 성공 (22 routes)

> **참고:** 통합 테스트는 Docker 네트워크 내부(`db`, `redis` 서비스명으로 연결)에서만 정상 실행됨.
> EC2 또는 `docker-compose run backend pytest` 환경에서 실행해야 한다.

---

## 3. 남은 작업

### 3.1 구현 관련 완료 항목 이력

#### RP 기능 DB 마이그레이션 ✅
#### 페르소나 카테고리 필드 ✅
#### 모바일 반응형 ✅
#### TTS 실제 구현 ✅
#### AI 이미지 생성 실제 구현 ✅
#### E2E 테스트 (Playwright) 확대 ✅
#### CI/CD 파이프라인 보안 스캐닝 추가 ✅
#### Redis 영구 저장 설정 ✅
#### 캐릭터 페이지 시스템 (Phase 1-5) ✅
#### 토론 플랫폼 (Phase 1-6) ✅
- 에이전트 CRUD + 버전 관리 + 로컬/외부 에이전트 지원
- 토픽 CRUD + 카테고리 필터링
- 매치 관리 + LLM 기반 토론 엔진
- WebSocket 실시간 토론 (로컬 에이전트 WebSocket 포함)
- 에이전트 템플릿 시스템 (3개 초기 템플릿, 커스터마이징 스키마)
- Tool Executor (dice, fact_check 등)
- Human Detection (토론 참여자 인간/AI 감지)

#### SSE 스트리밍 수정 ✅
- PII 파이프라인 spaCy 초기화 실패 시 graceful degradation
- event_generator try/except 추가 (27초 지연 문제 해결)
- Next.js API 라우트 프록시 방식으로 전환 (`/api/[...path]/route.ts`)

#### 보안 강화 ✅
- 회원가입/닉네임 변경 시 'admin' 포함 닉네임 차단 (`backend/app/api/auth.py`)

#### CLAUDE.md 문서 분할 ✅
- `CLAUDE.md` (루트) — 프로젝트 개요, 아키텍처, 배포 정보
- `backend/CLAUDE.md` — Python 컨벤션, DB, 테스트 규칙
- `frontend/CLAUDE.md` — TypeScript 컨벤션, Live2D, 컴포넌트 규칙

### 3.2 외부 의존 (운영 전 설정 필요)

#### 성인인증 실제 인증사 연동
- **파일:** `backend/app/services/adult_verify_service.py`
- **현재 상태:** 테스트용 검증 로직 구현 완료 (생년 기반 연령 확인, 인증코드 123456)
- **프로덕션 전환 시:**
  - 휴대폰 본인인증 API 연동 (KG이니시스/나이스 등)
  - 카드사 인증 API 연동
  - OAuth SSO 연동
- **의존:** 인증 서비스 업체 계약 및 API 키 발급

#### HTTPS/SSL 적용
- **현재 상태:** nginx HTTP-only 운영 중 (포트 80)
- **프로덕션 전환 시:**
  - SSL 인증서 발급 (Let's Encrypt 또는 ACM)
  - nginx HTTPS 설정 + HTTP→HTTPS 리다이렉트
  - docker-compose.prod.yml nginx 포트 443 추가
- **의존:** 도메인 등록

#### RunPod 추론 엔드포인트
- **현재 상태:** OpenAI API(GPT-4o-mini)로 대체 운영 중
- **프로덕션 전환 시:**
  - RunPod 계정 + A100 80GB GPU 엔드포인트 설정
  - `RUNPOD_API_KEY`, `RUNPOD_ENDPOINT_ID` 환경변수 설정
- **의존:** RunPod 계정 + GPU 엔드포인트

#### Live2D 에셋 준비
- **현재 상태:** PixiJS + pixi-live2d-display SDK 통합 완료, 모션/감정 매핑 코드 구현
- **필요:**
  - Cubism SDK 4 라이센스 취득
  - Live2D 모델 파일(.model3.json, .moc3) 제작 또는 구매
  - 배경 이미지 에셋 준비

---

## 4. EC2 운영 현황

| 항목 | 내용 |
|---|---|
| EC2 IP | `54.180.202.169` (Elastic IP 없음, 재시작 시 변경됨) |
| SSH 키 | `~/Downloads/chatbot-key.pem` |
| 배포 경로 | `/opt/chatbot` (docker-compose.prod.yml) |
| 배포 명령 | `./deploy.sh` |
| 운영 서비스 | db, redis, backend, frontend, nginx (5개, 관측성 스택 제외) |
| 등록된 LLM | GPT-4o-mini (OpenAI, llm_models 테이블) |
| admin 계정 | nickname: admin, password: admin123 |
| 스트리밍 | SSE 정상 동작 확인 (한국어 토큰 스트리밍) |

> **주의:** EC2는 Elastic IP가 없으므로 stop/start 시 Public IP가 변경됨.
> 재시작 후 반드시 새 IP를 확인하고 SSH 연결 및 배포 스크립트에 반영할 것.

---

## 5. 사용자가 직접 설정해야 할 사항

### 5.1 환경 변수 파일 생성 (필수)

`.env.example`을 복사하여 `.env` 파일을 생성하고 실제 값을 입력해야 합니다.

```bash
cp .env.example .env
```

#### 환경 변수 목록

| 변수 | 설명 | 기본값 / 비고 |
|---|---|---|
| **DATABASE_URL** | PostgreSQL 비동기 접속 URL | `postgresql+asyncpg://chatbot:chatbot@localhost:5432/chatbot` |
| **DATABASE_SYNC_URL** | PostgreSQL 동기 접속 URL (Alembic용) | `postgresql://chatbot:chatbot@localhost:5432/chatbot` |
| **REDIS_URL** | Redis 접속 URL | `redis://localhost:6379/0` |
| **SECRET_KEY** | JWT 서명용 시크릿 키 | 반드시 랜덤 문자열로 변경 (`openssl rand -hex 32`) |
| **ACCESS_TOKEN_EXPIRE_MINUTES** | JWT 토큰 만료 시간(분) | `60` |
| **RUNPOD_API_KEY** | RunPod API 키 | RunPod 대시보드에서 발급 |
| **RUNPOD_ENDPOINT_ID** | RunPod Serverless 엔드포인트 ID | 엔드포인트 생성 후 확인 |
| **OPENAI_API_KEY** | OpenAI API 키 | GPT-4o 사용 시 필요 |
| **ANTHROPIC_API_KEY** | Anthropic API 키 | Claude 사용 시 필요 |
| **GOOGLE_API_KEY** | Google AI API 키 | Gemini 사용 시 필요 |
| **LANGFUSE_PUBLIC_KEY** | Langfuse 퍼블릭 키 | self-hosted 사용 시 Langfuse UI에서 발급 |
| **LANGFUSE_SECRET_KEY** | Langfuse 시크릿 키 | self-hosted 사용 시 Langfuse UI에서 발급 |
| **LANGFUSE_HOST** | Langfuse 호스트 URL | `http://localhost:3001` |
| **SENTRY_DSN** | Sentry DSN URL | 선택 (없으면 에러 추적 비활성) |
| **APP_ENV** | 앱 환경 | `development` |
| **DEBUG** | 디버그 모드 | `true` |
| **CORS_ORIGINS** | 허용 CORS 출처 | `http://localhost:3000` |
| **RATE_LIMIT_ENABLED** | Rate Limiting 활성화 | `true` |
| **QUOTA_ENABLED** | 사용량 할당 활성화 | `true` |
| **DEFAULT_DAILY_TOKEN_LIMIT** | 기본 일일 토큰 한도 | `100000` |
| **DEFAULT_MONTHLY_TOKEN_LIMIT** | 기본 월간 토큰 한도 | `2000000` |
| **DEFAULT_MONTHLY_COST_LIMIT** | 기본 월간 비용 한도 ($) | `10.0` |

> **참고:** LLM API 키는 최소 1개 이상 설정해야 채팅 기능이 동작합니다.

### 5.2 Docker 서비스 구동 (필수)

```bash
# 로컬 개발: DB + Redis만 구동
docker-compose up -d db redis

# 관측성 포함: DB + Redis + Prometheus + Grafana + Langfuse
docker-compose up -d db redis prometheus grafana langfuse

# 전체 서비스 구동
docker-compose up -d
```

### 5.3 Alembic 마이그레이션 실행 (필수)

DB 컨테이너 구동 후 마이그레이션을 실행하여 테이블 + 인덱스를 생성합니다.

```bash
cd backend
alembic upgrade head
```

마이그레이션 이력 (18개):
- `a3b4fcc66da6` — pgvector 확장 + 17개 테이블 생성
- `b1c2d3e4f5a6` — usage_quotas 테이블 추가
- `c2d3e4f5a6b7` — 크레딧/구독 테이블 추가
- `d3e4f5a6b7c8` — 커뮤니티 게시판 테이블 추가
- `e4f5a6b7c8d9` — 에이전트 시스템 테이블 추가
- `f5a6b7c8d9e0` — RP 기능 테이블/컬럼 추가
- `a1b2c3d4e5f6` — chat_sessions.status CHECK에 'deleted' 추가
- `b2c3d4e5f6a7` — 페르소나 카테고리 컬럼 추가
- `c3d4e5f6a7b8` — 페르소나 익명 설정 추가
- `d4e5f6a7b8c9` — LLM 모델 크레딧 필드 추가
- `e5f6a7b8c9d0` — 비디오 생성 테이블 추가
- `f6a7b8c9d0e1` — 페르소나 신고 시스템 추가
- `g7b8c9d0e1f2` — 사용자 임시 차단(banned_until) 추가
- `h8c9d0e1f2g3` — 캐릭터 페이지 시스템 테이블 추가
- `i9d0e1f2g3h4` — 캐릭터 페이지 크레딧 비용 매핑 추가
- `j0e1f2g3h4i5` — 토론 플랫폼 테이블 추가 (debate_agents, debate_matches, debate_topics, debate_turn_logs)
- `k1f2g3h4i5j6` — Human Detection 테이블 추가
- `l2g3h4i5j6k7` — 에이전트 템플릿 테이블 추가 (debate_agent_templates) + 시드 데이터

### 5.4 Python 환경 (권장)

- **Python 3.12+** 및 venv 사용 권장
- 테스트 실행 전 반드시 venv 활성화 필요 (`backend/.venv/Scripts/activate` on Windows)
- Docker 배포 환경은 Python 3.12 사용

### 5.5 Live2D 에셋 준비 (Live2D 사용 시)

```
frontend/public/assets/
├── live2d/
│   └── {모델명}/
│       ├── {모델명}.model3.json
│       ├── {모델명}.moc3
│       ├── {모델명}.physics3.json
│       ├── textures/
│       └── motions/
└── backgrounds/
    ├── default.jpg
    └── {추가 배경 이미지들}
```

- **Cubism SDK 4 라이센스** — Live2D Inc.에서 별도 취득 필요

### 5.6 외부 서비스 가입

| 서비스 | 용도 | 필수 여부 | 비고 |
|---|---|:---:|---|
| **RunPod** | Llama 3 70B 추론 | LLM 중 택 1 | 핸들러 구현 완료, 배포만 필요 |
| **OpenAI** | GPT-4o 모델 | 선택 | EC2에서 GPT-4o-mini 사용 중 |
| **Anthropic** | Claude 모델 | 선택 | API 키만 설정하면 즉시 사용 |
| **Google AI** | Gemini 모델 | 선택 | API 키만 설정하면 즉시 사용 |
| **Langfuse** | LLM 관측성 | 선택 | self-hosted (docker-compose 포함) |
| **Sentry** | 에러 추적 | 선택 | DSN 설정 시 활성화 |

---

## 6. 로컬 개발 실행 방법

### 빠른 시작

```bash
# 1. 환경 변수 설정
cp .env.example .env

# 2. Docker 서비스 (DB + Redis) 구동
docker-compose up -d db redis

# 3. DB 마이그레이션
cd backend
alembic upgrade head

# 4. 백엔드 실행 (venv 활성화 후)
source backend/.venv/bin/activate  # Linux/Mac
# backend\.venv\Scripts\activate  # Windows
pip install -r requirements-dev.txt
uvicorn app.main:app --reload --port 8000

# 5. 프론트엔드 실행 (별도 터미널)
cd frontend
npm install
npm run dev
```

### 테스트 실행

```bash
# 반드시 venv 활성화 후 실행
source backend/.venv/bin/activate  # Linux/Mac
# backend\.venv\Scripts\activate  # Windows

# 백엔드 단위 테스트만 (317개, 호스트에서 실행 가능)
pytest backend/tests/unit/ -v

# 백엔드 통합 테스트 (Docker DB 환경 필요)
# docker-compose run --rm backend pytest backend/tests/integration/ -v
# 또는 CI 환경에서 실행

# 프론트엔드 컴포넌트 테스트 (37개)
cd frontend && npx vitest run

# E2E 테스트 (~111개, API 모킹 기반)
cd frontend && npx playwright test
```

---

## 7. 아키텍처 요약

```
┌─ 프론트엔드 (Next.js 15) ─────────────────────────────────────┐
│  Tailwind CSS + Dynamic Import + React.memo                    │
│  Live2D (PixiJS + pixi-live2d-display + Cubism 4)             │
│  SSE 프록시 (Next.js API Route → FastAPI)                      │
│  토론 플랫폼 UI (AgentForm, RankingTable, TurnBubble 등)       │
│  Playwright E2E ~111개 + Vitest 37개                           │
└───────────────────────┬────────────────────────────────────────┘
                        │ HTTP + SSE (현재 HTTP-only)
┌───────────────────────▼────────────────────────────────────────┐
│  EC2 t4g.small (서울) — 54.180.202.169                          │
│  ┌─ FastAPI ────────────────────────────────────────────────┐  │
│  │  143개 API 엔드포인트 + 토론 WebSocket                    │  │
│  │  Rate Limiting (Redis 슬라이딩 윈도우)                    │  │
│  │  Usage Quota (일일/월간 토큰+비용 한도)                   │  │
│  │  토론 엔진 (멀티 에이전트 + 템플릿 시스템)                 │  │
│  │  Human Detection (인간/AI 감지)                           │  │
│  │  단위 테스트 317개 통과                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌─ PostgreSQL + pgvector ──┐  ┌─ Redis ────────────────────┐  │
│  │  36개 테이블              │  │  세션/캐시/Rate Limit      │  │
│  │  Langfuse DB (별도 DB)   │  │  RDB + AOF 영구 저장        │  │
│  └──────────────────────────┘  └────────────────────────────┘  │
└───────────────────────┬────────────────────────────────────────┘
                        │ HTTP (RTT ~150ms)
┌───────────────────────▼────────────────────────────────────────┐
│  RunPod Serverless (미국) / 외부 LLM API                        │
│  현재 EC2: OpenAI GPT-4o-mini 사용 중                           │
│  RunPod SGLang (Llama 3 70B) ← 미배포                          │
└────────────────────────────────────────────────────────────────┘
```

---

## 8. CI/CD 파이프라인

`.github/workflows/ci.yml` — Push/PR 시 자동 실행:

| Job | 내용 |
|---|---|
| `backend-lint` | ruff check + format |
| `backend-unit-test` | pytest unit |
| `backend-integration-test` | pytest integration + pgvector 서비스 |
| `frontend-lint` | ESLint + Prettier |
| `frontend-test` | Vitest (37개) |
| `frontend-build` | next build (lint+test 성공 후) |
| `codeql-analysis` | CodeQL SAST (JS/TS + Python) |
| `dependency-review` | PR 시 의존성 취약점 검토 |
| `python-security` | safety + bandit |
| `npm-audit` | npm audit (high 이상) |
| `secret-scanning` | TruffleHog 시크릿 탐지 |
