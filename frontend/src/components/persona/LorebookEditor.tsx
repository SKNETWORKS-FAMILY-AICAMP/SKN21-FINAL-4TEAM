'use client';

type Props = {
  personaId: string;
};

export function LorebookEditor({ personaId }: Props) {
  return (
    <div className="lorebook-editor">
      {/* 로어북 항목 CRUD (제목, 본문, 태그) */}
    </div>
  );
}
