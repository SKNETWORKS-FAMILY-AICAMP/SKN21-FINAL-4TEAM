'use client';

type Props = {
  sessionId: string;
};

export function ChatWindow({ sessionId }: Props) {
  return (
    <div className="chat-window">
      {/* 메시지 리스트 + SSE 스트리밍 표시 */}
    </div>
  );
}
