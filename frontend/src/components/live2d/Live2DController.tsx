'use client';

type Props = {
  emotionMappings: Record<string, string>;
  currentEmotion: string;
};

export function Live2DController({ emotionMappings, currentEmotion }: Props) {
  return null;
  // 감정 → 모션 매핑 제어 로직
}
